{
  "name": "@cardverse/db",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "generate": "prisma generate",
    "push": "node prisma/run-prisma.mjs db push",
    "migrate": "node prisma/run-prisma.mjs migrate dev",
    "deploy": "node prisma/run-prisma.mjs migrate deploy",
    "studio": "prisma studio",
    "consolidate-users": "node --env-file=../../.env prisma/consolidate-users.mjs",
    "build": "prisma generate && tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"no lint\""
  },
  "dependencies": {
    "@prisma/client": "^6.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "prisma": "^6.2.1",
    "typescript": "^5.7.2"
  }
}
